# Fase 6 — Lacunas UX: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Polir 4 fricções UX documentadas: login `?next=` ignorado, busca ausente na lista de usuários, menu de usuário inacessível (hover-only), e placeholders `VERIFY` pendentes no README/docs.

**Architecture:** Quatro correções independentes. (1) `signInAction` passa a ler e validar `next` do formData e redirecionar pra lá. (2) Lista de usuários ganha input de busca via query param `?q=`, filtrando no Supabase com `ilike`. (3) `UserMenu` vira client component com state controlado, `aria-haspopup`/`aria-expanded`, fechamento por clique-fora/Esc. (4) Placeholders `VERIFY` substituídos pela URL de produção confirmada.

**Tech Stack:** Next.js App Router (server components + server actions + um client component novo), TypeScript.

## Global Constraints

- O `next` do login deve ser **validado**: path relativo começando com `/`, e **não** pode ser `/login` ou `/signup` (evitar redirect loop).
- A busca de usuários usa `ilike` (case-insensitive) sobre `full_name`, `username` e `email`. Sem paginação virtual (YAGNI para centenas de usuários).
- O `UserMenu` precisa continuar funcionando visualmente idêntico ao atual — só o comportamento muda de hover → click.
- Reusar o padrão visual de input de busca de `components/posts/post-filters.tsx`.
- `signOut` no `UserMenu` hoje duplica `signOutAction` de `app/actions/auth.ts` — unificar (usar a action compartilhada).

---

## File Structure

- **Modify:** `app/actions/auth.ts` — `signInAction` lê/valida `next`.
- **Modify:** `app/(admin)/admin/users/page.tsx` — aceita `searchParams`, filtra por `q`.
- **Create:** `components/admin/user-search.tsx` — client component do input de busca (padrão `PostFilters`).
- **Modify:** `components/layout/user-menu.tsx` — vira client component acessível.
- **Modify:** `README.md`, `docs/ARCHITECTURE.md`, `docs/DEPLOYMENT.md` — resolver placeholders `VERIFY`.

---

## Task 1: Login `?next=` — fazer `signInAction` respeitar o destino

**Files:**
- Modify: `app/actions/auth.ts`
- Test: `tests/lib/actions/auth.test.ts` (estender)

**Interfaces:**
- Consumes: `next` já enviado como hidden input pelo `LoginForm` (chave `"next"` no formData).
- Produces: `signInAction` redireciona para o `next` validado, ou `/timeline` como fallback.

- [ ] **Step 1: Adicionar helper de validação de `next` e usar no `signInAction`**

Em `app/actions/auth.ts`, substituir a função `signInAction` inteira por:

```typescript
/** Validates a post-login redirect path: must be relative, not an auth route. */
function safeRedirectPath(next: string | null | undefined): string {
  if (!next) return '/timeline';
  // Must be an absolute path (starts with /) but not a protocol-relative URL (//evil.com).
  if (!next.startsWith('/') || next.startsWith('//')) return '/timeline';
  // Avoid redirect loops: never allow landing back on auth pages.
  if (next === '/login' || next === '/signup' || next.startsWith('/auth/')) return '/timeline';
  return next;
}

export async function signInAction(_prev: ActionResult | null, formData: FormData): Promise<ActionResult> {
  const parsed = loginSchema.safeParse({
    email: formData.get('email'),
    password: formData.get('password'),
  });
  if (!parsed.success) return { ok: false, error: 'Email ou senha inválidos' };
  const supabase = await createClient();
  const { error } = await supabase.auth.signInWithPassword(parsed.data);
  if (error) return { ok: false, error: 'Email ou senha inválidos' };
  redirect(safeRedirectPath(formData.get('next') as string | null));
}
```

Notas:
- `next.startsWith('//')` bloqueia URLs protocol-relative (`//evil.com`) — vetor de redirect aberto.
- `next === '/login'` etc. previne loop.
- `formData.get('next')` pode ser `null` (form sem o hidden input) — `safeRedirectPath` trata.

- [ ] **Step 2: Adicionar testes para `safeRedirectPath` (via signInAction)**

Se `tests/lib/actions/auth.test.ts` existir, estender; senão criar. Os testes devem mockar `@/lib/supabase/server` (já é o padrão em `tests/lib/actions/`) e `next/navigation` (`redirect`).

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const signInMock = vi.fn();
vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(async () => ({
    auth: { signInWithPassword: signInMock },
  })),
}));
const redirectMock = vi.fn((path: string) => {
  // next/navigation's redirect throws NEXT_REDIRECT in real runtime; mock a throw
  // so the action behaves like it does in prod. But for unit-testing the path
  // argument, we capture it and NOT throw, then assert it was called.
});
vi.mock('next/navigation', () => ({ redirect: redirectMock }));

import { signInAction } from '@/app/actions/auth';

describe('signInAction redirect target', () => {
  beforeEach(() => {
    signInMock.mockReset();
    redirectMock.mockReset();
    signInMock.mockResolvedValue({ error: null });
  });

  it('redirects to next path when valid', async () => {
    await signInAction(null, new FormData());
    // Test the safeRedirectPath logic indirectly via the redirect arg.
  });

  it('falls back to /timeline when next is missing', async () => {
    const fd = new FormData();
    fd.set('email', 'a@b.com');
    fd.set('password', 'password123');
    await signInAction(null, fd);
    expect(redirectMock).toHaveBeenCalledWith('/timeline');
  });

  it('uses next when it is a valid relative path', async () => {
    const fd = new FormData();
    fd.set('email', 'a@b.com');
    fd.set('password', 'password123');
    fd.set('next', '/post/abc');
    await signInAction(null, fd);
    expect(redirectMock).toHaveBeenCalledWith('/post/abc');
  });

  it('rejects protocol-relative URLs (open redirect)', async () => {
    const fd = new FormData();
    fd.set('email', 'a@b.com');
    fd.set('password', 'password123');
    fd.set('next', '//evil.com');
    await signInAction(null, fd);
    expect(redirectMock).toHaveBeenCalledWith('/timeline');
  });

  it('rejects auth routes to avoid loops', async () => {
    const fd = new FormData();
    fd.set('email', 'a@b.com');
    fd.set('password', 'password123');
    fd.set('next', '/login');
    await signInAction(null, fd);
    expect(redirectMock).toHaveBeenCalledWith('/timeline');
  });
});
```

- [ ] **Step 3: Rodar os testes**

Run: `pnpm test tests/lib/actions/auth.test.ts`
Expected: todos passam.

- [ ] **Step 4: Typecheck**

Run: `pnpm typecheck`
Expected: passa.

- [ ] **Step 5: Commit**

```bash
git add app/actions/auth.ts tests/lib/actions/auth.test.ts
git commit -m "fix(auth): respect ?next= redirect after login (with open-redirect guard)"
```

---

## Task 2: Busca na lista de usuários (admin)

**Files:**
- Modify: `app/(admin)/admin/users/page.tsx` — aceitar `searchParams.q` e filtrar.
- Create: `components/admin/user-search.tsx` — client component do input.

**Interfaces:**
- Consumes: `requireAdmin`, `createClient`, padrão visual de `PostFilters`.
- Produces: lista filtrável por `?q=` (ilike sobre full_name/username/email).

- [ ] **Step 1: Criar `components/admin/user-search.tsx`**

Client component que atualiza a URL com `?q=` (mesmo padrão de `PostFilters`):

```typescript
'use client';

import { useCallback } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { Search } from 'lucide-react';
import { cn } from '@/lib/utils';

export function UserSearch() {
  const router = useRouter();
  const sp = useSearchParams();
  const current = sp.get('q') ?? '';

  const update = useCallback(
    (value: string) => {
      const next = new URLSearchParams(sp.toString());
      if (value) next.set('q', value);
      else next.delete('q');
      const qs = next.toString();
      router.push(qs ? `/admin/users?${qs}` : '/admin/users');
    },
    [router, sp]
  );

  return (
    <div className="relative">
      <Search className="absolute left-4 top-1/2 -translate-y-1/2 h-4 w-4 text-text-muted" />
      <input
        type="search"
        placeholder="Buscar por nome, usuário ou email…"
        defaultValue={current}
        onBlur={(e) => update(e.target.value)}
        onKeyDown={(e) =>
          e.key === 'Enter' && update((e.target as HTMLInputElement).value)
        }
        aria-label="Buscar usuários"
        className={cn(
          'w-full h-12 pl-11 pr-4 rounded-full bg-surface-elevated border border-border-subtle',
          'text-body text-text-primary placeholder:text-text-muted',
          'transition-all duration-base ease-out-quart',
          'focus:outline-none focus:border-brand-azul focus:shadow-glow-azul'
        )}
      />
    </div>
  );
}
```

- [ ] **Step 2: Atualizar `app/(admin)/admin/users/page.tsx` para filtrar por `q`**

Substituir o conteúdo da page por:

```typescript
import { requireAdmin } from '@/lib/auth';
import { createClient } from '@/lib/supabase/server';
import { UserRow } from '@/components/admin/user-row';
import { UserSearch } from '@/components/admin/user-search';
import { EmptyState } from '@/components/ui/empty-state';
import { Users } from 'lucide-react';

export default async function AdminUsersPage({
  searchParams,
}: {
  searchParams: Promise<{ q?: string }>;
}) {
  const me = await requireAdmin();
  const { q } = await searchParams;
  const trimmed = q?.trim();

  const supabase = await createClient();
  let query = supabase
    .from('profiles')
    .select('*')
    .order('created_at', { ascending: false })
    .limit(500);

  if (trimmed) {
    const like = `%${trimmed}%`;
    query = query.or(`full_name.ilike.${like},username.ilike.${like},email.ilike.${like}`);
  }

  const { data: users } = await query;
  const list = (users ?? []) as unknown as Parameters<typeof UserRow>[0]['user'][];

  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-h1 text-text-primary">Usuários</h1>
        <p className="text-body text-text-secondary mt-1">
          {list.length} colaborador(es){trimmed ? ` para "${trimmed}"` : ''}.
        </p>
      </div>
      <UserSearch />
      {list.length === 0 ? (
        <EmptyState icon={<Users className="h-10 w-10" />} title="Nenhum usuário encontrado" />
      ) : (
        <ul className="space-y-3">
          {list.map((u) => <UserRow key={u.id} user={u} isMe={u.id === me.id} />)}
        </ul>
      )}
    </div>
  );
}
```

Notas:
- `.or()` com `ilike` faz a busca case-insensitive sobre os 3 campos de uma vez.
- `trimmed` evita busca com whitespace.
- A contagem exibida reflete o resultado filtrado.

- [ ] **Step 3: Verificar se `profiles` tem coluna `email`**

Run: `grep -rn "email" supabase/migrations/0001_init.sql | head -5`
Confirmar se a tabela `profiles` tem `email` (pode vir do auth.users via FK, não como coluna direta). Se `email` **não** for coluna de `profiles`, remover `,email.ilike.${like}` do `.or()` (buscar só por `full_name` e `username`). Verificar empiricamente: o `select('*')` atual retorna email? Se o componente `UserRow` mostra email, a coluna existe; se não mostra, pode não existir.

- [ ] **Step 4: Typecheck + Build**

Run: `pnpm typecheck && pnpm build`
Expected: ambos passam.

- [ ] **Step 5: Commit**

```bash
git add app/(admin)/admin/users/page.tsx components/admin/user-search.tsx
git commit -m "feat(admin): add user search by name/username/email"
```

---

## Task 3: Menu de usuário acessível (click + aria + Esc)

**Files:**
- Modify: `components/layout/user-menu.tsx`

**Interfaces:**
- Consumes: `signOutAction` de `@/app/actions/auth` (unifica o signOut duplicado).
- Produces: `UserMenu` client component com state, `aria-haspopup`/`aria-expanded`, fechamento por clique-fora e Esc.

- [ ] **Step 1: Reescrever `components/layout/user-menu.tsx`**

```typescript
'use client';

import { useState, useRef, useEffect } from 'react';
import { signOutAction } from '@/app/actions/auth';
import { LogOut, User as UserIcon, Settings, ChevronDown } from 'lucide-react';
import Link from 'next/link';
import { Avatar } from '@/components/ui/avatar';
import { cn } from '@/lib/utils';
import type { User } from '@supabase/supabase-js';

type Props = {
  user: User;
  fullName: string;
  avatarUrl?: string | null;
};

export function UserMenu({ user, fullName, avatarUrl }: Props) {
  const [open, setOpen] = useState(false);
  const containerRef = useRef<HTMLDivElement>(null);

  // Close on click outside.
  useEffect(() => {
    if (!open) return;
    function onPointerDown(e: PointerEvent) {
      if (containerRef.current && !containerRef.current.contains(e.target as Node)) {
        setOpen(false);
      }
    }
    document.addEventListener('pointerdown', onPointerDown);
    return () => document.removeEventListener('pointerdown', onPointerDown);
  }, [open]);

  // Close on Escape.
  useEffect(() => {
    if (!open) return;
    function onKeyDown(e: KeyboardEvent) {
      if (e.key === 'Escape') setOpen(false);
    }
    document.addEventListener('keydown', onKeyDown);
    return () => document.removeEventListener('keydown', onKeyDown);
  }, [open]);

  return (
    <div className="relative" ref={containerRef}>
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        className="flex items-center gap-2 rounded-full p-1 pr-3 hover:bg-surface-sunken transition-colors duration-base ease-out-quart"
        aria-label="Menu do usuário"
        aria-haspopup="menu"
        aria-expanded={open}
      >
        <Avatar size="sm" name={fullName} src={avatarUrl ?? null} />
        <span className="hidden md:inline text-body-sm font-medium text-text-primary">
          {fullName.split(' ')[0]}
        </span>
        <ChevronDown
          className={cn(
            'hidden md:inline h-3.5 w-3.5 text-text-muted transition-transform duration-base',
            open && 'rotate-180'
          )}
        />
      </button>
      {open && (
        <div
          role="menu"
          className="absolute right-0 top-full mt-2 w-64 rounded-2xl bg-surface-elevated shadow-lg border border-border-subtle p-2 z-50 transition-all duration-base ease-out-quart"
        >
          <div className="p-3 mb-1 rounded-xl bg-surface-sunken/60">
            <p className="text-body-sm font-semibold text-text-primary">{fullName}</p>
            <p className="text-caption text-text-muted truncate">{user.email}</p>
          </div>
          <Link
            href="/meu-desempenho"
            role="menuitem"
            onClick={() => setOpen(false)}
            className="flex items-center gap-3 px-3 py-2.5 rounded-xl text-body-sm font-medium text-text-primary hover:bg-surface-sunken transition-colors"
          >
            <UserIcon className="h-4 w-4 text-text-muted" /> Meu desempenho
          </Link>
          <Link
            href="/meu-desempenho#perfil"
            role="menuitem"
            onClick={() => setOpen(false)}
            className="flex items-center gap-3 px-3 py-2.5 rounded-xl text-body-sm font-medium text-text-primary hover:bg-surface-sunken transition-colors"
          >
            <Settings className="h-4 w-4 text-text-muted" /> Configurações
          </Link>
          <div className="my-1 border-t border-border-subtle" />
          <form action={signOutAction}>
            <button
              type="submit"
              role="menuitem"
              className="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-body-sm font-medium text-state-error-strong hover:bg-state-error/8 transition-colors"
            >
              <LogOut className="h-4 w-4" /> Sair
            </button>
          </form>
        </div>
      )}
    </div>
  );
}
```

Notas:
- Vira `'use client'` para ter state.
- `signOut` local duplicado removido — usa `signOutAction` de `app/actions/auth`.
- `aria-haspopup="menu"` + `aria-expanded={open}` no trigger.
- `role="menu"` no container, `role="menuitem"` nos itens.
- Fecha por clique-fora (`pointerdown` no document) e Esc.
- Chevron gira quando `open` (em vez de `group-hover`).
- Visual mantém as mesmas classes de styling.

- [ ] **Step 2: Verificar que `UserMenu` é consumido por um server component (não quebra a árvore)**

Run: `grep -rn "UserMenu" --include="*.tsx" app components`
Confirmar onde é usado (provavelmente `components/layout/header.tsx` ou similar). Como vira client component, o parent server component precisa passar as props (`user`, `fullName`, `avatarUrl`) serializáveis — `User` do Supabase e strings são serializáveis. **Não deve haver problema**, mas confirmar que o parent não passa funções não-serializáveis.

- [ ] **Step 3: Typecheck + Build**

Run: `pnpm typecheck && pnpm build`
Expected: ambos passam.

- [ ] **Step 4: Commit**

```bash
git add components/layout/user-menu.tsx
git commit -m "fix(a11y): make user menu click-toggle with aria support and Esc/click-outside"
```

---

## Task 4: Resolver placeholders `VERIFY` no README e docs

**Files:**
- Modify: `README.md`
- Modify: `docs/ARCHITECTURE.md`
- Modify: `docs/DEPLOYMENT.md`

**Interfaces:**
- Consumes: URL de produção confirmada `https://sebraiers.jhonatansoares.com` (de `.gsd/DECISIONS.md`).

- [ ] **Step 1: Localizar todos os placeholders `VERIFY`**

Run: `grep -rn "VERIFY" README.md docs/ARCHITECTURE.md docs/DEPLOYMENT.md`
Confirmar as linhas exatas (README:76, ARCHITECTURE:~148, DEPLOYMENT:~64).

- [ ] **Step 2: Substituir o placeholder do README (linha 76)**

Trocar:
```
... bindings. <!-- VERIFY: URL de produção do Worker no Cloudflare -->
```
por:
```
... bindings. Produção: https://sebraiers.jhonatansoares.com
```

- [ ] **Step 3: Tratar os placeholders de `docs/ARCHITECTURE.md` e `docs/DEPLOYMENT.md`**

Para cada placeholder encontrado:
- Se for sobre **URL de produção**: substituir por `https://sebraiers.jhonatansoares.com`.
- Se for sobre **outros detalhes não-confirmáveis** (ex.: "exact Cloudflare account/project name", "R2 bucket region and dashboard creation steps"): esses não temos como preencher com certeza — **remover o comentário `VERIFY`** e deixar a frase adjacente como está (ou suavizar para indicar que são detalhes de configuração do ambiente). Não inventar valores.

Ler o contexto ao redor de cada um antes de decidir.

- [ ] **Step 4: Confirmar que não sobrou nenhum `VERIFY`**

Run: `grep -rn "VERIFY" README.md docs/ARCHITECTURE.md docs/DEPLOYMENT.md || echo "LIMPO"`
Expected: `LIMPO` (ou só ocorrências em arquivos que decidimos deixar, documentadas no relatório).

- [ ] **Step 5: Commit**

```bash
git add README.md docs/ARCHITECTURE.md docs/DEPLOYMENT.md
git commit -m "docs: resolve VERIFY placeholders with confirmed production URL"
```

---

## Task 5: Gate final da fase (e do projeto)

**Files:**
- Verify: estado do repositório.

- [ ] **Step 1: Typecheck**

Run: `pnpm typecheck`
Expected: passa sem erros.

- [ ] **Step 2: Testes**

Run: `pnpm test`
Expected: todos passam (com os novos testes do signInAction).

- [ ] **Step 3: Build**

Run: `pnpm build`
Expected: build conclui sem erros.

- [ ] **Step 4: Lint**

Run: `pnpm lint`
Expected: sem erros.

- [ ] **Step 5: Validação manual recomendada (documentar, não automatizar)**

Estes pontos exigem interação humana/visual:
- Login: acessar uma rota protegida deslogado → cai em `/login?next=/rota` → logar → deve ir para `/rota` (não `/timeline`).
- Busca de usuários: como admin, digitar no input → URL fica `/admin/users?q=termo` → lista filtra.
- Menu: clicar no avatar → menu abre; clicar fora ou Esc → fecha; navegar com Tab → foco entra no menu.

---

## Notas

- Esta fase é a última das 6. Resolve as lacunas pequenas documentadas em `.superpowers/sdd/progress.md` pelo build original.
- A validação de `next` (Task 1) fecha um vetor de **open redirect** que existia latente — bom capturar.
- A busca de usuários (Task 2) reusa o padrão visual de `PostFilters` para consistência.
- O `UserMenu` (Task 3) vira client component — confirma que o parent não passa props não-serializáveis. O `signOut` duplicado é unificado.
- Os placeholders `VERIFY` (Task 4) são pendências de documentação que ficaram do build original; alguns só podem ser preenchidos por quem conhece a config do Cloudflare (você) — nesses casos removemos o comentário sem inventar valores.
